import styled from "styled-components";

export const FullScreenButtonWrapper = styled.div`
  position: absolute;
  bottom: 1rem;
  left: 1rem;
  z-index: 10;
  cursor: pointer;
  color: white;
  font-size: 2rem;
`;
